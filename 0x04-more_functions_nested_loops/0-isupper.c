#include "main.h"
#include <stdio.h>
/**
 *_isupper - a function that test if upper or not
 *@n: the character
 *Return: 0 or 1
*/
int _isupper(int n)
{
if (n >= 65 && n <= 90)
{
return (1);
}
else
{
return (0);
}
}
