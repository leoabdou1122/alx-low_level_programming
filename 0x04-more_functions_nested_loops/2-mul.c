#include "main.h"
#include <stdio.h>
/**
 *mul - a function that multilple two variable
 *@a: the fist number
 *@b: the second number
 *Return: s
*/
int mul(int a, int b)
{
int s;
s = a * b;
return (s);
}
