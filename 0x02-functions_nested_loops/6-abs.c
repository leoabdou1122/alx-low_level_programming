#include "main.h"
#include <stdio.h>
/**
 *_abs - a function that computes the absolute value
 *@r: the integer
 *Return: 0
*/
int _abs(int r)
{
if (r < 0)
{
r *= -1;
return (r);
}
else
{
return (r);
}
}
