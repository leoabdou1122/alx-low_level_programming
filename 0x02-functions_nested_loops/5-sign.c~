#include "main.h"
#include <stdio.h>
/**
 *print_sign - a function that print the sign of the number
 *@n: the number
 *Return: 0, 1 or -1
*/
int print_sign(int n)
{
if (n > 0)
{
_putchar ('+');
return (1);
}
else if (n < 0)
{
_putchar ('-');
return (-1);
}
else
{
_putchar (48);
return (0);
}
