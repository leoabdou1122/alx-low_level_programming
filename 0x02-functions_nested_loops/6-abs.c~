#include "main.h"
#include <stdio.h>
/**
 *_abs - a function that computes the absolute value
 *Return: 0
*/
int _abs(int)
{
int r;
if (r < 0)
{
r *= -1;
printf("%d", r);
}
else
{
printf("%d", r);
}
return (0);
}
