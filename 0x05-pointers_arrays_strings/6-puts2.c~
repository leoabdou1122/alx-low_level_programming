#include "main.h"
#include <stdio.h>

/**
 *puts2 - a function .
 *@str: the string
*/
void puts2(char *str)
{
int i;
int l;
l = strlen(str);
for (i = 0; i < l; i += 2)
{
printf("%s", str[i]);
}
printf("\n");
}
